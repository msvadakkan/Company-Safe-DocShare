<?xml version="1.0" encoding="utf-8"?>
<!--
    Allows HTTP to 10.0.2.2 (Android emulator localhost) for development.
    Remove the <domain-config> block and set cleartextTrafficPermitted="false"
    when deploying to production with HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow plain HTTP to emulator host during development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
</network-security-config>
